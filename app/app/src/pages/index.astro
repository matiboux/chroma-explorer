---
import { site } from '~/site'
import Layout from '~/layouts/Layout.astro'
import LocaleSwitch from '~/components/LocaleSwitch.astro'
import Login from '~/components/Login.svelte'
import Footer from '~/components/Footer.astro'

import { i18nFactory } from '~/i18n'
import type { Locales } from '~/i18n'
const _ = i18nFactory(Astro.currentLocale as Locales | undefined)
---

<Layout
	bodyClass="bg-gray-100 h-screen"
>

	<div class="flex flex-col justify-center items-center min-h-full overflow-y gap-8 p-8 w-full max-w-[calc(800px+2*2rem)] mx-auto">
		<div class="bg-white shadow-lg rounded-lg p-6 w-full space-y-4">
			<div class="flex items-start justify-between">
				<h1 class="text-4xl font-bold">
					{site.title}
				</h1>
				<LocaleSwitch />
			</div>
			{site.description &&
				<p class="text-gray-600">
					{_(site.description)}
				</p>
			}

			<Login client:visible locale={Astro.currentLocale} />
		</div>

		<Footer class="w-full" />
	</div>

</Layout>
